# nelmwave build artifacts (anywhere, incl. examples/)
.nelmwave/

# kubeconfig written by the e2e docker-compose fixture
test/e2e/.kube/

# compiled binary
/nelmwave
/dist/

# Go
*.test
*.out
coverage.txt

# editor / OS
.DS_Store
.idea/
.vscode/

# graphify: the knowledge graph is a local, regenerable artifact — every
# contributor builds their own (`graphify update .`, AST-only, no API key).
# .claude/settings.json is tracked and machine-independent; keep personal
# overrides in .claude/settings.local.json.
graphify-out/
.claude/settings.local.json
